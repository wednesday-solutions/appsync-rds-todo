#set( $deletedAt = "deleted_at" )
#set( $statement = "UPDATE notes set $deletedAt=NOW() WHERE id=$ctx.args.id" )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE users set $deletedAt=NOW() WHERE id=$ctx.args.id", "SELECT * FROM users WHERE id=$ctx.args.id"]
}